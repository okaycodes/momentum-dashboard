// // Listen for new tabs.
// chrome.tabs.onCreated.addListener(function (tab) {
//   if (tab.url === "chrome://newtab/") {
//     alert("new tab");
//     chrome.tabs.update(tab.id, { url: "https://okenana.vercel.app" });
//   }
// });

console.log("launch dashboard");
